# Database (Supabase Postgres pooler)
DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD@db.wovwqhejawtrscsjkkdi.supabase.co:5432/postgres

# Supabase Auth (JWT 검증용 — JWKS)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
SUPABASE_JWT_SECRET=  # 남기고 싶으면 유지, JWKS 기반이라 빈 값 OK

# OpenAI
OPENAI_API_KEY=sk-...

# Storage
STORAGE_BUCKET=instagram-dashboard

# 초대 링크 base (Vercel 프론트 URL)
APP_URL=http://localhost:3000

# SMTP (옵션 — 비우면 초대 API가 이메일 대신 invite_url을 응답으로 반환)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=True

DEBUG=True
